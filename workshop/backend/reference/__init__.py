"""Bundled reference solutions.

These make the whole day run end-to-end before any student has dropped their
own work into ``workshop/solutions/``. The engine prefers a student solution
when present (see ``backend/loader.py``) and falls back to these.
"""
