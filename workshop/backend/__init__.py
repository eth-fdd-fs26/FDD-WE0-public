"""Nuclear Central Manager — backend package.

A small FastAPI app + a step-based "day engine" that runs the three homework
solutions (pump failure, basin temperature, waste-machine clustering) as one
gamified shift. See workshop/README.md.
"""
